stopifnot(sum(grep("ppv", readLines("question7.R"))) > 0)
stopifnot(sum(grep("sensitivity", readLines("question7.R"))) > 0)
stopifnot(sum(grep("specificity", readLines("question7.R"))) > 0)
stopifnot(sum(grep("prevalence", readLines("question7.R"))) > 0)
