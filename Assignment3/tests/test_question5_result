stopifnot(sum(grep("hist()", readLines("question5.R"))) > 0)
stopifnot(sum(grep("apply()", readLines("question5.R"))) + sum(grep("rowMeans()", readLines("question5.R"))) + sum(grep("mean", readLines("question5.R"))) > 0)
stopifnot(sum(grep("var", readLines("question5.R"))) > 0)
